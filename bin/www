#!/usr/bin/env node

const http = require('http')
  , app = require('../')
  , port = process.env.PORT || 3000

http.createServer(app.callback())

app.listen(port)

console.log(`Tags API - port ${port}`)
